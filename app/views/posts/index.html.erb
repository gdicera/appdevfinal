<div>
  <div>
    <h1>
      List of all posts
    </h1>
  </div>
</div>

<hr>
<div>
  <div>
    <table border="1">
      <tr>
        <th>User</th>
        <th>Topic</th>
        <th>Body</th>
        <th>Created at</th>
        <th>Updated at</th>
      </tr>

      <% @list_of_posts.each do |a_post| %>
      <tr>
        <td><%= a_post.id %></td>
        <td><%= a_post.user_id %></td>
        <td><%= a_post.topic_id %></td>
        <td><%= a_post.body %></td>
        <td><%= time_ago_in_words(a_post.created_at) %> ago</td>
        <td><%= time_ago_in_words(a_post.updated_at) %> ago</td>
        <td><a href="/posts/<%= a_post.id %>">Show details</a></td>
      </tr>
      <% end %>
    </table>
  </div>
</div>

<hr>
